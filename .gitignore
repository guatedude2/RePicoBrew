node_modules

/.cache
/.react-router
/build
/public/build
/public/recipe-photos/*
!/public/recipe-photos/.gitkeep
.env
pnpm-debug.log*
.pnpm-store/**
public/_proto*
prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
prisma/*.db-shm

# pi-image build scratch space: downloaded base images (hundreds of MB) and in-progress/output
# .img files (multiple GB) — never meant to be committed, see pi-image/README.md.
/pi-image/cache
/pi-image/work

# Local secrets and data dumps — never commit these (connection strings, user tables, request logs).
/prisma/conn.txt
/prisma/users-backup-*.sql
*.log
